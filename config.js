function Config(){
	var cols = 30; // number of colums
	var rows = 20; // number ofConfig.rows
	var scl = 20; // scale = size of the cells in px
	var Config.show_nextgen_helper = true;
	var Config.show_neighbors_count = false;
	var Config.drawFrameRate = 30;
}
