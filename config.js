class Config{
	constructor(){
		const cols = 30; // number of colums
		const rows = 20; // number of rows
		const scl = 20; // scale = size of the cells in px
		let show_nextgen_helper = true;
		let show_neighbors_count = false;
		let drawFrameRate = 30;
	}
}
